[workspace]
members = [
  "server",
  "types",
  "client",
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1.0.*", features = ["derive"] }
ndarray = { version = "0.15.6", features = ["serde"] }
itertools = "0.10.0"
