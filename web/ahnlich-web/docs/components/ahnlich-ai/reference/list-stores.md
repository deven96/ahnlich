---
title: LISTSTORES
sidebar_label: LISTSTORES
---

# LISTSTORES

Display AI stores in a schema. Omit `SCHEMA` to list `public` only.

## Syntax

```bash
LISTSTORES
LISTSTORES SCHEMA media
```

## Example

```bash
LISTSTORES SCHEMA media
```

---

See [SDK examples](/docs/stores/list-stores) (choose the **AI proxy** tab) for Python, Node, Go, and Rust.
