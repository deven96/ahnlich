---
title: DROPSCHEMA
sidebar_label: DROPSCHEMA
---

# DROPSCHEMA

Remove a non-public schema **and every store inside it**. The `public` schema cannot be dropped.

## Syntax

```bash
DROPSCHEMA <schema>
```

## Example

```bash
DROPSCHEMA analytics
```

---

See [SDK examples](/docs/concepts/schemas) for Python, Node, Go, and Rust.
