---
title: Ping the DB server
sidebar_label: Ping the DB server
---

# Ping the DB server

Check if the DB server is alive and responding.

Run it after connecting the CLI with `--agent db`.

```bash
PING
```
