---
title: List DB stores
sidebar_label: List DB stores
---

# List DB stores

List stores in a schema (omit `SCHEMA` for `public`).

Run it after connecting the CLI with `--agent db`.

```bash
LISTSTORES
LISTSTORES SCHEMA analytics
```
