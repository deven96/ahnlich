---
title: Query by predicate
sidebar_label: Query by predicate
---

# Query by predicate

Retrieve all entries matching a metadata condition.

Run it after connecting the CLI with `--agent db`.

```bash
GETPRED (author = Alice) IN my_store
```
