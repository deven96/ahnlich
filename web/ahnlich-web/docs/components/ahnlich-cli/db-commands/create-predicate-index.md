---
title: Create predicate index
sidebar_label: Create predicate index
---

# Create predicate index

Index metadata fields to speed up predicate queries.

Run it after connecting the CLI with `--agent db`.

```bash
CREATEPREDINDEX (author, category) IN my_store
```
