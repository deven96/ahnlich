---
title: Drop a schema
sidebar_label: Drop a schema
---

# Drop a schema

Drop a non-public schema and all stores inside it. `public` cannot be dropped.

Run it after connecting the CLI with `--agent db`.

```bash
DROPSCHEMA analytics
```
