---
title: Create non-linear index
sidebar_label: Create non-linear index
---

# Create non-linear index

Build an HNSW index for faster similarity search.

Run it after connecting the CLI with `--agent db`.

```bash
CREATENONLINEARALGORITHMINDEX (hnsw) IN my_store
```
