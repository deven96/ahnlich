---
title: Drop non-linear index
sidebar_label: Drop non-linear index
---

# Drop non-linear index

Drop a non-linear (HNSW) index.

Run it after connecting the CLI with `--agent ai`.

```bash
DROPNONLINEARALGORITHMINDEX (hnsw) IN my_store
```
