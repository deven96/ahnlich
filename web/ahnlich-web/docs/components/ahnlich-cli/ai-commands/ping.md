---
title: Ping the AI server
sidebar_label: Ping the AI server
---

# Ping the AI server

Check if the AI server is alive and responding.

Run it after connecting the CLI with `--agent ai`.

```bash
PING
```
