---
title: Drop a schema
sidebar_label: Drop a schema
---

# Drop a schema

Drop a non-public schema and all AI stores inside it (also drops the backing DB schema). `public` cannot be dropped.

Run it after connecting the CLI with `--agent ai`.

```bash
DROPSCHEMA media
```
