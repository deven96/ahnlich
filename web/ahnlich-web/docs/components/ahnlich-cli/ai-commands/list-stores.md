---
title: List AI stores
sidebar_label: List AI stores
---

# List AI stores

List stores in a schema (omit `SCHEMA` for `public`).

Run it after connecting the CLI with `--agent ai`.

```bash
LISTSTORES
LISTSTORES SCHEMA media
```
