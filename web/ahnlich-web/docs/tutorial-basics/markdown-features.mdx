---
sidebar_position: 4
---

# Libraries


ahnlich-client-rs: Rust client for ahnlich-db and ahnlich-ai with support for connection pooling

export const Highlight = ({children, color}) => (
  <button
    style={{
      backgroundColor: '#1589ba',
      borderRadius: '10px',
      color: '#fff',
      padding: '10px',
      cursor: 'pointer'
    }}
    onClick={() => {
      alert(`You clicked the ${children} button`);
    }}>
    {children}
  </button>
);

<div
  style={{
    display: 'flex',
    gap: '1rem',
    margin: '0 0 10px 0'
  }}
>
  <Highlight>See Example</Highlight>
  <Highlight>See Github</Highlight>
  <Highlight>Docs</Highlight>
</div>

ahnlich-client-py: Python client for ahnlich-db and ahnlich-ai with support for connection pooling.

### Using Poetry
```
poetry add ahnlich-client-py
```

### Using Pip
```
pip3 install ahnlich-client-py
```
